---
import BentoItem from '@components/BentoItem.astro'
import { projects } from '@cv'
const [bingo, buscaM, myPlato, ecomm, moviRent] = projects
---

<section
  class='snap-start animate-fade-up animate-once relative w-full max-w-3xl grid grid-cols-10 auto-rows-[15rem] gap-2 mx-auto px-4 py-2'
>
  <BentoItem
    class='col-span-10 md:col-span-4 transition-scale duration-1000 ease-in-out'
    title={buscaM.name}
    description={buscaM.description}
    tech={buscaM.highlights}
    linkRepo={buscaM.repo}
    linkDemo={buscaM.demo}
  >
    <!-- Group para que reconozca el hover de un item a su contenedor (Article del Bento) -->
    <div
      slot='image'
      class='absolute left-0 top-0 bottom-0 h-full w-full group-hover:scale-110
    transition-scale duration-1000 ease-in-out opacity-90 bg-center bg-cover bg-no-repeat bg-blend-luminosity -z-10'
      style=`background-image: url(${buscaM.imgPrev}.webp)`
    >
    </div>
  </BentoItem>

  <BentoItem
    class='col-span-10 md:col-span-6'
    title={moviRent.name}
    description={moviRent.description}
    tech={moviRent.highlights}
    linkRepo={moviRent.repo}
    linkDemo={moviRent.demo}
  >
    <div
      slot='image'
      class='absolute left-0 top-0 bottom-0 h-full w-full group-hover:scale-110 transition-scale duration-1000 ease-in-out opacity-90 bg-center bg-cover bg-no-repeat bg-blend-luminosity -z-10'
      style=`background-image: url(${moviRent.imgPrev}.webp)`
    >
    </div>
  </BentoItem>

  <BentoItem
    class='col-span-10 md:col-span-6'
    title={ecomm.name}
    description={ecomm.description}
    tech={ecomm.highlights}
    linkRepo={ecomm.repo}
  >
    <div
      slot='image'
      class='absolute left-0 top-0 bottom-0 h-full w-full group-hover:scale-110 transition-scale duration-1000 ease-in-out opacity-90 bg-center bg-cover bg-no-repeat bg-blend-luminosity -z-10'
      style=`background-image: url(${ecomm.imgPrev}.webp)`
    >
    </div>
  </BentoItem>

  <BentoItem
    class='col-span-10 md:col-span-4 transition-scale duration-1000 ease-in-out'
    title={bingo.name}
    description={bingo.description}
    tech={bingo.highlights}
    linkRepo={bingo.repo}
    linkDemo={bingo.demo}
  >
    <!-- Group para que reconozca el hover de un item a su contenedor (Article del Bento) -->
    <div
      slot='image'
      class='absolute left-0 top-0 bottom-0 h-full w-full group-hover:scale-110
    transition-scale duration-1000 ease-in-out opacity-90 bg-center bg-cover bg-no-repeat bg-blend-luminosity -z-10'
      style=`background-image: url(${bingo.imgPrev}.webp)`
    >
    </div>
  </BentoItem>
</section>
